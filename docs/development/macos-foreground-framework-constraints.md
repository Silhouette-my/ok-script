# Deprecated documentation filename

The normative reusable-platform contract for the OK-WW macOS foreground MVP is:

```text
docs/development/macos-foreground-platform-constraints.md
```

This file is retained only as a discoverability pointer for references created during Stage 0. It defines no independent requirements and must not be cited as a separate contract. Update new references to use the canonical filename above.
